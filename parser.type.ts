import { Scanner } from "./scanner";
import {
  SimpleType,
  SIMPLE_TYPE_KEYWORDS,
  Punctuator,
  BinaryOperator,
  Token,
} from "./scanner.func";

export function createTypeParser(scanner: Scanner) {
  // asdads
}
